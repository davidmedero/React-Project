import React from "react";
import MakeupList from "./Makeup";
console.log(MakeupList);
function Wishlist(props) {
  return <div>text</div>;
}

export default Wishlist;

// {MakeupList.map((eachProduct) => {
//     <div>{eachProduct.name}</div>;
//   })}
